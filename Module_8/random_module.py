import random

# print(random.randint(1, 10))

# nums = [random.randint(1, 100) for i in range(20)]
# print(nums)
# random.shuffle(nums)  # randomly shuffles a list
# print(nums)

# nums = [random.randint(1, 100) for i in range(20)]
# choice = random.choice(nums)  # randomly selects 1 item
# print(choice)
# choices_nu = random.choices(nums, k=25)  # randomly selects non-unique items, the number of which is specified in 'k='
# print(choices_nu)
# choices_u = random.sample(nums, k=10)  # randomly selects unique items, the number of which is specified in 'k='
# # choices_u = random.sample(nums, k=21)  # will cause a ValueError
# print(choices_u)

# random.seed(42)  # sets the random seed for consistency, the result will always be the same
# nums = [random.randint(1, 100) for i in range(20)]
# print(nums)
# random.shuffle(nums)
# print(nums)
# choices = random.choices(nums, k=10)
# print(choices)
