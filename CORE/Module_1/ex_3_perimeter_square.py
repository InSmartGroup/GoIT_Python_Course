# Given the side length of a square, calculate its perimeter

side_length = float(input("Enter the side length: "))

perimeter = round(side_length * 4, 2)

print(f"The perimeter of a square is {perimeter}")
